# Server-side only — set this in Vercel → Project → Settings → Environment Variables.
# Get a key at https://console.anthropic.com (enable Web Search under org settings).
ANTHROPIC_API_KEY=sk-ant-...
